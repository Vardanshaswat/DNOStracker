{
  "name": "dnostracker",
  "version": "1.0.0",
  "private": true,
  "description": "Hourly habit tracker for health, work, prep, workout, social, stress, and study — with a flexible sleep window.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
